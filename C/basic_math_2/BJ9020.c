#include <stdio.h>

//골드바흐의 추측
//2보다 큰 짝수는 두 소수의 합으로 나타낼 수 있다.
int main()
{
    int Ncase = 0;
    int Ninput = 0;

    scanf("%d",&Ncase);
    for (int n = 0; n < Ncase ; n++)
    {
        scanf("%d",Ninput);
        
    }

}