#include <stdio.h>

int main()
{
    int a, b, c, d = 0;
    int output = 0;
    int flag = 0; 
    scanf("%d", &a);

    for (int i = 0; (i * 3) < a; i++)
    {
        for (int j = 0; (j * 3) < a; j++)
        {

        }
    }
}